/**
 * 1. var : 더 이상 사용하지 않는다.
 * 2. let : 
 * 3. const : 
 */
var TestName = 3;
console.log(TestName);

let Ive = '아이브';
console.log(Ive);

Ive = '안유진';

TestName = 1;

TestName = undefined;

if(undefined != null)
{
    console.log('null == undefined');
}